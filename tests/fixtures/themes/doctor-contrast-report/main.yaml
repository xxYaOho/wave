theme:
  color:
    primary:
      main:
        $type: color
        $value: "#aaaaaa"
      on-main:
        $type: color
        $value: "#ffffff"
doctor:
  wcagPairs:
    primary-main:
      foreground: "{theme.color.primary.on-main}"
      background: "{theme.color.primary.main}"
